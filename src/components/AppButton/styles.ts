import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    borderRadius: 28,
    marginTop: 20,
  },

  content: {
    height: 56,
    justifyContent: 'center',
  },

  label: {
    fontWeight: '700',
    letterSpacing: 1,
    fontSize: 14,
  },
});
